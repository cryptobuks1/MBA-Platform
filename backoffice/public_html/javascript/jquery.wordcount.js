jQuery.fn.wordCount = function(params) {    var p=            {                counterElement: "display_count"            };    var a=            {                countersmsElement: "display_smscount"            };    var total_words;    if (params) {        jQuery.extend(p, params);    }    //for each keypress function on text areas    this.keypress(function()    {        total_words = this.value.length;                total_words += 1;        total_words_count = total_words-1;        jQuery('#' + p.counterElement).html(total_words);        if (total_words > 161)        {            total_sms = Math.floor(total_words_count / 160) + 1;        }        else {            total_sms = Math.floor(total_words / 161) + 1;        }        jQuery('#' + a.countersmsElement).html(total_sms);        document.send_sms.sms_count.value = total_sms;    });};